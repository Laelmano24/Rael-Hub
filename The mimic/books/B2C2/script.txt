-- Cria uma ScreenGui
local screenGui = Instance.new("ScreenGui")
screenGui.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")

-- Cria o TextLabel
local textLabel = Instance.new("TextLabel")
textLabel.Size = UDim2.new(0, 600, 0, 50)  -- Ajusta o tamanho para o texto
textLabel.Position = UDim2.new(0.5, -300, 0.5, -25)  -- Centraliza a posição
textLabel.Text = "The script for this chapter is under maintenance. Please wait. If you want to receive notifications from Rael Hub: https://rael-hub-site.vercel.app"
textLabel.TextColor3 = Color3.fromRGB(255, 255, 255)  -- Cor do texto (branco)
textLabel.BackgroundTransparency = 1  -- Sem fundo
textLabel.TextSize = 20
textLabel.TextWrapped = true  -- Quebra de linha automática se o texto for muito longo
textLabel.TextStrokeTransparency = 0  -- Define a transparência do contorno
textLabel.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)  -- Cor do contorno (preto)
textLabel.Font = Enum.Font.SourceSansBold  -- Define a fonte
textLabel.Parent = screenGui